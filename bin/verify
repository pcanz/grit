#!/bin/bash

# cd is assumed to be parent of the test/ directory..

cd test  # restored at end

node test-all.js > results

diff -b results verified

echo test/results were compared to: test/verified

cd -  # retore cd
